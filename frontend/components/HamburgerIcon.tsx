import React from 'react';

// This component is not actively used, as HamburgerIcon is imported from Icons.tsx.
// It is provided as an empty stub to ensure all .tsx files are valid JavaScript/TypeScript.
const HamburgerIcon: React.FC = () => {
  return null;
};

export default HamburgerIcon;